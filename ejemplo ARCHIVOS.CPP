#include <stdio.h>
#include <conio.h>
#include <iostream.h>


main()
{
cin<<alumno.carnet;
gets(alumno.nombre);
FILE * archivo
archivo = fopen("alumno.dat",a);
fwrite(&alumno,sizeof(alumno), 1, archivo);
fclose(archivo);
}